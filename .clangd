CompileFlags:
  Add: 
    - "--gcc-toolchain=/usr"  # GCC 工具链根目录（通常为 /usr）
    - "-isystem/usr/include/c++/11"  # 显式指定 GCC 的 C++ 头文件路径（需替换为你的 GCC 版本）
    - "-isystem/usr/include/x86_64-linux-gnu/c++/11"  # 平台相关头文件路径（可选）
  
  QueryDriver: 
    - "/usr/bin/g++"  # 指定 GCC 路径